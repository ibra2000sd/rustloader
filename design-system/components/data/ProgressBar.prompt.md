State-colored progress track (yt-dlp path, small files). Segmented direct downloads use SegmentBar.

```jsx
<ProgressBar value={62} status="downloading" />
<ProgressBar value={100} status="complete" />
```
