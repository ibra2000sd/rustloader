Button with rust primary, machined secondary, ghost, destructive variants; verb-first labels.

```jsx
<Button variant="primary" size="lg" icon="download">Download</Button>
<Button size="sm" icon="pause">Pause</Button>
<Button variant="destructive" size="sm" icon="x">Cancel</Button>
```

One primary per view. Row controls use size="sm". Disabled greys fully.
