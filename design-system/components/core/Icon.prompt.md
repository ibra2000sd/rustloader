Stroke icon glyph (embedded Lucide paths) — the only sanctioned iconography; inherits `currentColor`.

```jsx
<Icon name="download" size={16} />
<Icon name="pause" color="var(--warning-400)" />
```

Names: download, arrow-down-to-line, pause, play, x, check, folder, folder-open, settings, history, clipboard, link, alert-triangle, rotate-cw, trash, chevron-down, zap, clock, gauge, inbox, plus, search, layers.
