Panel/card surface with hairline + machined inset highlight; `glass` only for overlays.

```jsx
<Card>…</Card>
<Card elevation="panel" accent padding={24}>hero</Card>
<Card glass>clipboard banner</Card>
```
