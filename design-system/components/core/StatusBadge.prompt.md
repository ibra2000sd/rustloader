Status tag for the download state machine (queued → downloading → complete, plus paused/stalled/error).

```jsx
<StatusBadge status="downloading" />
<StatusBadge status="downloading" label="Retrying…" />
```
