{
  "manifest_version": 3,
  "name": "Rustloader SPIKE probe — overlay strategy (a) diagnostics (NEVER SHIP)",
  "version": "0.0.1",
  "description": "Throwaway instrumentation for docs/spike-overlay-a-findings.md. Load unpacked, run the site matrix, paste the dumps, delete. Branch spike/overlay-a-investigation only — never merge.",
  "permissions": ["webRequest", "webNavigation", "storage"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "probe-worker.js",
    "type": "module"
  },
  "content_scripts": [
    {
      "matches": ["http://*/*", "https://*/*"],
      "js": ["probe-frame.js"],
      "all_frames": true,
      "match_about_blank": true,
      "run_at": "document_idle"
    }
  ],
  "action": {
    "default_title": "Dump probe report for this tab (opens nothing — see the probe's service-worker console)"
  }
}
